[project]
name = "my-backend-project"
version = "0.1.0"
description = "A public source-available backend"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "YOUR-CUSTOM-SOURCE-AVAILABLE-LICENSE" }

# List all your production dependencies here
dependencies = [
    "django>=6.1.1",
    "uvicorn>=0.28.0",
]

[tool.setuptools]
packages = []